import React from "react";
import { useParams } from "react-router-dom";
function Home() {
  //   let { id } = useParams();
  //   return <div>This is the home page. Welcome {id}</div>;
  return <div>This is the home page. Welcome!</div>;
}

export default Home;
